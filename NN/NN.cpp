#include "stdafx.h"
#include "NN.h"

NN::NN(rx::DataSet& dataset): dataset(dataset), set(set)
{
}

//void NN::addInputLayer(int inputSize, int outputSize)
//{
//	this->input_layer = new InputLayer(inputSize, outputSize, set.)
//}
